"""
CascadeX FastAPI dependency injection — stub module.

This module is intentionally minimal in Phase 01.
`get_current_user` and `require_permission` are implemented in Phase 03
once auth and RBAC are built.

Do not add business logic here until Phase 03.
"""

# TODO: Phase 03 — implement get_current_user(token: str = Depends(oauth2_scheme))
# TODO: Phase 03 — implement require_permission(level: PermissionLevel) factory dependency
