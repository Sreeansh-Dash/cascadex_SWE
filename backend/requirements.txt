# CascadeX Backend Dependencies

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.6

# Database
neo4j>=6.3.0

# Config / validation
pydantic>=2.9.2
pydantic-settings>=2.5.2
python-dotenv>=1.0.1
email-validator>=2.1.0

# Auth (Phase 03)
argon2-cffi>=25.1.0
# Migrated from python-jose (unmaintained, CVE-flagged) to PyJWT (actively maintained)
PyJWT>=2.9.0
# passlib is no longer needed — password hashing is done entirely via argon2-cffi

# Rate limiting (Phase 03)
slowapi>=0.1.9

# HTTP client
httpx>=0.28.1

# Testing
pytest>=8.3.3
pytest-asyncio>=0.24.0
anyio>=4.7.0

# Linting
ruff>=0.6.9

# Phase 08: PDF export (history)
reportlab>=4.2.5
pypdf>=6.16.2

# Phase 09: LLM (Gemini API) + field-level encryption
# Migrated from deprecated google-generativeai (EOL Nov 2025) to google-genai (new unified SDK)
google-genai>=1.0.0
cryptography>=42.0.0
